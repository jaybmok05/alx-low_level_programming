#include "main.h"

/**
 * _islower - this checks if letter is
 * lowercase
 *
 * Return: returns 1 if lower case, 0 if not.
 *
 * @c: this is the parameter.
 */

int _islower(int c)
{
	if (c == 'c')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
