#ifndef MAIN_FILE_H
#define MAIN_FILE_H

/**
 * main - tests 0-positive_or_negative function
 * in case of 0.
 *
 * largest_number() - finds the largest number out
 * of three
 * Return: returns 0 when successful.
 */

int main(void);/*function prototype for 0-positive_or_negative.c*/
int largest_number(int a, int b, int c);/*function prototype*/
int convert_day(int month, int day);/*function prototype*/
void print_remaining_days(int month, int day, int year);
#endif
