#include "main.h"

/**
 * reverse_array - this function
 * reerves an array
 * @a: 1st parameter.
 * @n: second parameter.
 */

void reverse_array(int *a, int n)
{
	int k;
	n = sizeof(a) / sizeof(a[0]);

	for (k = n - 1; k >= 0; k++)
	{}
}
